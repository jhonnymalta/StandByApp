@model StandBy.Web.DTOs.ClienteDTO
@{
    ViewData["Title"] = "Excluir cliente";
    Layout = "_Layout_Loged";
}

<div>
    <h2 class="text-center text-gray-500 text-2xl mb-4">Tem certeza que deseja excluir o Cliente: <span class="text-3xl font-bold">@Model.Nome</span></h2>
    <div>
        <form asp-controller="Clientes" asp-action="Delete" asp-route-id="@Model.Id" method="post" class="text-center">
            <input type="hidden" asp-for="Id"  class="mt-4"/>
            <input type="submit" value="Excluir" class="btn btn-danger" />
            <a class="btn btn-info" href="javascript:window.history.back();">Voltar</a>
        </form>
    </div>
</div>

