@model StandBy.Web.DTOs.PedidoDTO;
@{
    ViewData["Title"] = "Descrição do Pedido";
    Layout = "_Layout_Loged";
}

<div>
    <h2 class="text-center text-4xl text-gray-500 mb-8">Descritivo do Pedido N°: @Model.Id</h2>
</div>
<div>
   <ul class="nav nav-tabs">
  <li class="nav-item">

  </li>
  <li class="nav-item">

  </li>
  </div>

<div>
    <a  asp-controller="PedidosItens" asp-action="Create" asp-route-id="@Model.Id" class="btn btn-primary">Inserir um novo item</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Código Do Prodtudo</th>            
            <th scope="col">Quantidade</th>
            <th scope="col">Valor</th>
            <th scope="col">Ações</th>
        </tr>
    </thead>
    <tbody>

        @* @foreach (var item in Model.PedidosItens)
        {
                    <tr>
                    <th scope="row">1</th>
                    <td>@item.ProdutoID</td>
                    <td>@item.Quantidade</td>
                    <td>@item.ValorUnitario</td>
                    <td>@item.ValorTotal</td>
                    <td>
                            <a asp-controller="PedidosItems" asp-action="Index" asp-route-id="@item.Id" class="btn btn-warning">Ver</a>
                            <a asp-controller="PedidosItems" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
                    </td>
                    </tr>
        }        *@

    </tbody>
</table>
